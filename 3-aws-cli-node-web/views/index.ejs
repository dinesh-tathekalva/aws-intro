<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Test</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="domain" type="text" class="ghost-input" placeholder="Enter a Domain Name" required>
          <input type="submit" class="ghost-button" value="Check">
        </form>
        <% if(domains !== null){ %> 
          <% for (var i = 0; i < domains.length; i++) { %>
            <div>
              Domain: <%= domains[i].domain %>
              Created On: <%= domains[i].createdOn %>
              Is Active: <%= domains[i].isActive %>
              Country: <%= domains[i].country %>
            </div>
          <% } %>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>
